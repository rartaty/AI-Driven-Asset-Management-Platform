import pandas as pd

def evaluate_long_term_fundamentals(df: pd.DataFrame) -> dict:
    """
    [PROPRIETARY LOGIC REDACTED]
    セキュリティおよび戦略上の優位性（アルファ）を保護するため、
    長期運用の適格性判定（FCFやEBITDAを用いた詳細な分析条件）は非公開としています。
    """
    return {
        "is_eligible": False,
        "assets_growth_rate_pct": 0.0,
        "ebitda_growth_rate_pct": 0.0,
        "reason": "Logic hidden for public release"
    }
